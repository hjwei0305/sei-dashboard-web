import React, { Component } from 'react';
import cls from 'classnames';
import styles from './index.less'

class TechBlue extends Component {

    render() {
        return (
            <div className={cls(styles['screen-box'])}>aa</div>
        )
    }
}

export default TechBlue;